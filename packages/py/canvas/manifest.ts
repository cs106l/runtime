import { Manifest } from "../../../bundler";

const manifest: Manifest = {
  name: "canvas",
  description: "Native support for HTML5 canvases",
  version: "0.1.0",
  files: ["**/*.py"],
};

export default manifest;